class Match {
    constructor(firstUserId, firstUserLike, secondUserId, secondUserLike) {
        this.firstUserId = firstUserId;
        this.firstUserLike = firstUserLike;
        this.secondUserId = secondUserId;
        this.secondUserLike = secondUserLike;
    }

    isMatch(firstUserId, secondUserId) {

    }
}

module.exports = Match;